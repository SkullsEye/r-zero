numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
lightgbm>=4.0
pytest>=7.4

# Optional. numba JIT-compiles the contagion scan and makes the offline
# reproduction about 14x faster on the hot loop (2.6s -> 0.19s over 600k
# events). Everything runs and all 145 tests pass without it, so it is kept
# out of the required list rather than risking a wheel that does not exist
# yet for your Python version.
#   pip install numba>=0.58
